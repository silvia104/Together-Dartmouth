<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/questionText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:textStyle="bold"
        android:background="@drawable/rectangle_question_background" />

    <ImageButton
        android:id="@+id/expand_arrow"
        android:layout_width="32dp"
        android:layout_height="30dp"
        android:layout_alignParentRight="true"
        android:background="@null"
        android:src="@drawable/ic_expand_more"
        android:tag="closed"
        android:scaleType="fitCenter"
        android:layout_alignTop="@+id/questionText"
        android:layout_marginTop="3dp"
        android:layout_marginRight="3dp" />

    <TextView
        android:id="@+id/answerText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text=""
        android:background="@drawable/rectangle_answer_background"
        android:layout_below="@+id/questionText" />
        <ImageButton
            android:id="@+id/editAnswer"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_edit"
            android:scaleType="centerInside"
            android:layout_marginRight="3dp"
            android:padding="6dp"
            android:background="@null"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="2dp"
            android:layout_alignBottom="@+id/answerText"
            android:layout_alignRight="@+id/answerText" />
</RelativeLayout>
